# SSH Key Generator - Windows Configuration# Windows-specific (for WSL2 or native PowerShell)

# Windows-specific settings (for WSL2 or native Windows)SSH_AGENT_PATH="C:\\Program Files\\Git\\usr\\bin\\ssh-agent.exe"

SSH_KEY_TIMEOUT=14400
# SSH Agent
SSH_AGENT_STARTUP="Start-Service ssh-agent"   # PowerShell command for Windows
SSH_AGENT_PATH="C:\\Program Files\\Git\\usr\\bin\\ssh-agent.exe"  # Git Bash ssh-agent

# Key Permissions
SSH_KEY_PERMS="600"                           # Permissions (not enforced on Windows)
SSH_DIR_PERMS="700"                           # Permissions (not enforced on Windows)

# Storage Location (WSL2)
SSH_KEYS_DEFAULT_DIR="~/.ssh"                 # Default WSL2 location
SSH_KEYS_WINDOWS_DIR="%USERPROFILE%\.ssh"    # Native Windows location

# System Tools
TAR_COMMAND="tar"                             # Available in WSL2 or Git Bash
OPENSSL_COMMAND="openssl"                     # Available via Git Bash or WSL2
SSH_KEYGEN_COMMAND="ssh-keygen"               # Available via Git Bash or WSL2

# Timeout (4 hours for Windows)
SSH_AGENT_TIMEOUT="14400"

# Dialog Settings
DIALOG_TYPE="tkinter"                         # Use tkinter for GUI

# Windows-Specific Configuration
WSL2_ENABLED="true"                           # Set to false for native Windows
GIT_BASH_PATH="C:\\Program Files\\Git\\bin"   # Path to Git Bash (if installed)

# Platform-Specific Notes:
# - Recommended: Use WSL2 for better compatibility
# - Git Bash is common alternative for native Windows
# - Windows Subsystem for Linux provides Linux environment
# - SSH keys typically stored in ~/.ssh (WSL2) or %USERPROFILE%\.ssh (native)
# - Process isolation on Windows provides good security
