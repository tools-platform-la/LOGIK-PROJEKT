# SSH Key Generator - Default Configuration# SSH Key Generator - Default Configuration

# This file contains default settings for SSH key generation# This file contains default settings for SSH key generation

# These values are used across all platforms unless overridden by OS-specific configs# These values are used across all platforms unless overridden by OS-specific configs



# Key Generation Settings# Key Generation Settings

SSH_KEY_TYPE_1="ed25519"           # Primary key type (modern, secure)SSH_KEY_TYPE_1="ed25519"           # Primary key type (modern, secure)

SSH_KEY_TYPE_2="rsa"               # Secondary key type (widely compatible)SSH_KEY_TYPE_2="rsa"               # Secondary key type (widely compatible)

SSH_KEY_BITS_RSA="4096"            # RSA key size in bitsSSH_KEY_BITS_RSA="4096"            # RSA key size in bits

SSH_KEY_COMMENT_FORMAT="{email}"   # Comment format for keys (uses {email} placeholder)SSH_KEY_COMMENT_FORMAT="{email}"   # Comment format for keys (uses {email} placeholder)



# Encryption Settings# Encryption Settings

ENCRYPTION_ALGORITHM="aes-256-cbc" # OpenSSL encryption algorithmENCRYPTION_ALGORITHM="aes-256-cbc" # OpenSSL encryption algorithm

PBKDF2_ITERATIONS="100000"         # Key derivation iterations (security)PBKDF2_ITERATIONS="100000"         # Key derivation iterations (security)

PASSPHRASE_MIN_LENGTH="12"         # Minimum passphrase lengthPASSPHRASE_MIN_LENGTH="12"         # Minimum passphrase length



# SSH Agent Settings# SSH Agent Settings

SSH_AGENT_TIMEOUT="14400"          # Default timeout in seconds (4 hours)SSH_AGENT_TIMEOUT="14400"          # Default timeout in seconds (4 hours)



# Directory Settings# Directory Settings

SSH_DIR="~/.ssh"                   # Location to store SSH keysSSH_DIR="~/.ssh"                   # Location to store SSH keys

BACKUP_EXT=".enc"                  # Extension for encrypted backupsBACKUP_EXT=".enc"                  # Extension for encrypted backups



# Logging Settings# Logging Settings

LOG_LEVEL="INFO"                   # Log level (DEBUG, INFO, WARNING, ERROR)LOG_LEVEL="INFO"                   # Log level (DEBUG, INFO, WARNING, ERROR)

LOG_FORMAT="%Y_%m_%d-%H_%M_%S"     # Timestamp format for logs and backupsLOG_FORMAT="%Y_%m_%d-%H_%M_%S"     # Timestamp format for logs and backups



# UI Settings# UI Settings

DIALOG_WIDTH="900"                 # Dialog window width in pixelsDIALOG_WIDTH="900"                 # Dialog window width in pixels

DIALOG_HEIGHT="300"                # Dialog window height in pixelsDIALOG_HEIGHT="300"                # Dialog window height in pixels

