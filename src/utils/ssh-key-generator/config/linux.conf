# SSH Key Generator - Linux Configuration# SSH Key Generator - Linux Configuration

# Linux-specific settings and overrides# Linux-specific settings and overrides



# SSH Agent# SSH Agent

SSH_AGENT_STARTUP="ssh-agent"      # Command to start SSH agent on LinuxSSH_AGENT_STARTUP="ssh-agent"      # Command to start SSH agent on Linux

SSH_AUTH_CHECK="pgrep -u $USER ssh-agent"  # Command to check if agent is runningSSH_AUTH_CHECK="pgrep -u $USER ssh-agent"  # Command to check if agent is running



# Key Permissions# Key Permissions

SSH_KEY_PERMS="600"                # Permissions for private keys (user read/write only)SSH_KEY_PERMS="600"                # Permissions for private keys (user read/write only)

SSH_DIR_PERMS="700"                # Permissions for ~/.ssh directorySSH_DIR_PERMS="700"                # Permissions for ~/.ssh directory



# Storage Location# Storage Location

SSH_KEYS_DEFAULT_DIR="~/.ssh"      # Default location for SSH keysSSH_KEYS_DEFAULT_DIR="~/.ssh"      # Default location for SSH keys



# System Tools# System Tools

TAR_COMMAND="tar"                  # Tar utility pathTAR_COMMAND="tar"                  # Tar utility path

OPENSSL_COMMAND="openssl"          # OpenSSL utility pathOPENSSL_COMMAND="openssl"          # OpenSSL utility path

SSH_KEYGEN_COMMAND="ssh-keygen"    # SSH-keygen utility pathSSH_KEYGEN_COMMAND="ssh-keygen"    # SSH-keygen utility path



# Timeout (4 hours for Linux)# Timeout (4 hours for Linux)

SSH_AGENT_TIMEOUT="14400"SSH_AGENT_TIMEOUT="14400"



# Dialog Settings# Dialog Settings

DIALOG_TYPE="tkinter"              # Use tkinter for GUI (built-in to Python)DIALOG_TYPE="tkinter"              # Use tkinter for GUI (built-in to Python)



# Platform-Specific Notes:# Platform-Specific Notes:

# - Uses standard GNU/Linux tools (tar, openssl, ssh-keygen)# - Uses standard GNU/Linux tools (tar, openssl, ssh-keygen)

# - SSH agent is typically started with: eval "$(ssh-agent -s)"# - SSH agent is typically started with: eval "$(ssh-agent -s)"

# - SSH keys stored in ~/.ssh by default# - SSH keys stored in ~/.ssh by default

# - Most distributions include openssh-client package# - Most distributions include openssh-client package

